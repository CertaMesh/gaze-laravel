# Plan

Superseded by the upstream v0.3 contract docs in `Naoray/gaze` and the repo migration notes in [`../MIGRATION-v0.3.md`](../MIGRATION-v0.3.md).

This repository now targets the `gaze clean` / `gaze restore` CLI contract rather than the earlier `ghostwriter` wrapper plan.
