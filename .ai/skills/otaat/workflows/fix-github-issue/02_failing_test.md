Read `.otat/context.md` and `.otat/reproduce.md`.

Your ONE job: write a failing test.

1. Check out a new branch: `otat-fix-<issue-id>`
2. Write a test that captures the bug — it must fail now and pass once the fix is in
3. Run the test suite to confirm it fails

Write nothing about solutions or fixes. Only the failing test.
