Read `.otat/context.md` for the issue description.

Your ONE job: reproduce the bug.

1. Do whatever is needed to reliably reproduce the issue (install packages, create test files, run commands)
2. Confirm you can reproduce it consistently
3. Write `.otat/reproduce.md` with:
   - Exact steps to reproduce (so a human could follow them)
   - What the expected behaviour is
   - What the actual (broken) behaviour is

Write nothing about solutions. Describe the problem only.
