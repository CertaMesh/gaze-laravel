Read `.otat/decision.md`, `.otat/problem.md`, and the codebase.

Your ONE job: lay everything out so the implementation is trivial.

Gather:
- The exact files that need to change (with paths)
- Relevant algorithms, patterns, or code from other parts of the repo that the implementer should follow
- Edge cases that must be handled
- Test cases the implementation must satisfy (beyond the failing test already written)
- Prior art in other projects worth referencing

Write all of this to `.otat/alleyoop.md`.

You are the prep cook. The next step is the surgeon. Do NOT implement anything.
